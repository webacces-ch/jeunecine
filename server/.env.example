# Copy to .env and adjust
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=cinema
PORT=8080
JWT_SECRET=change_this_secret
# Optional custom upload dir for article images
# ARTICLES_UPLOAD_DIR=/absolute/path/to/uploads/articles
